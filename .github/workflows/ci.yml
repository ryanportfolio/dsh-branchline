name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22.19.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run typecheck
      - run: pnpm test
      - run: pnpm run build
      - run: pnpm run pack:check
      - name: Verify companion plugins
        run: node scripts/verify-companion-plugins.mjs
      - name: Verify generated README
        if: runner.os == 'Linux'
        run: |
          pnpm run readme:build
          git diff --exit-code -- README.md assets/readme
      - name: Verify Windows launcher
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\test-launcher-upgrade.ps1
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          pwsh.exe -NoProfile -File .\scripts\test-launcher-upgrade.ps1
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\test-launcher-sync.ps1
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          pwsh.exe -NoProfile -File .\scripts\test-launcher-sync.ps1
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $env:DSH_LAUNCHER_HOME = Join-Path $env:RUNNER_TEMP 'dsh-launcher-selftest'
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\start-dsh.ps1 -SelfTest -Version '0.1.5-rc.2'
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          pwsh.exe -NoProfile -File .\start-dsh.ps1 -SelfTest -Version '0.1.5-rc.2'
